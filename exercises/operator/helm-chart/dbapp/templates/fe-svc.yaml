apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-fe-svc
  labels:
    day: day4
    sample: dbapp
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    day: day4
    sample: dbapp
    layer: frontend
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: adminer
      port: 8080
