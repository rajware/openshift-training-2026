apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: dbapp-operator-subscription
  namespace: openshift-operators
spec:
  channel: alpha
  name: operator
  source: dbapp-catalog
  sourceNamespace: openshift-marketplace
