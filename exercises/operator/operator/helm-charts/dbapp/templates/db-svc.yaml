apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db-svc
  labels:
    day: day4
    sample: dbapp
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    day: day4
    sample: dbapp
    layer: backend
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: mysql
      port: 3306
